# அறம் கோர் — AramCore

> A minimal observable x86 kernel with a live browser dashboard

AramCore is a real x86 kernel built from scratch in C and Assembly.
It runs inside QEMU and streams every internal kernel event — process
creation, memory allocation, state changes — in real time to a browser
dashboard.

---